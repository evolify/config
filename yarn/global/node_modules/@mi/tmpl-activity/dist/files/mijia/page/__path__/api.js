import React, {
  useEffect,
  useState,
  useContext
} from "react"
import axios from "axios"
import createStore from "./utils/store" 
import { toast } from "@mi/fit-ui"
import dayjs from "dayjs"

axios.defaults.baseURL = "/cgi-op/api/v1"

const STORAGE_KEY = "portrait_2020_agree"

const PAGE_SIZE = 13

export const agreed = Boolean(localStorage.getItem(STORAGE_KEY))

const initialData = {

}


export function useStore() {
  const [data, setData] = useState(initialData)
  const [loaded, setLoaded] = useState(false)

  const loadData = () => {
    console.log(1)
  }

  useEffect(() => {
    loadData()
  }, [])

  return {
    data,
    loaded
  }
}

export const {Provider, useCtx} = createStore(useStore)