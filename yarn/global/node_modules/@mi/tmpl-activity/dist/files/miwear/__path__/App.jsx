import React, { useEffect } from "react"
import miWear from "@mi/miwear-jssdk"
// 基础组件
import { toast } from "@mi/fit-ui"
import "./style.scss"

// 触发截屏操作
function screenShot() {
  toast("截屏")
  miWear.triggerShare({
    shareType: "screenshot",
  })
}

export default function App(props) {
  useEffect(() => {
    // 设置分享数据
    miWear.setShareData({
      title: "分享标题",
      description: "分享描述",
      url: window.location.href,
      imageUrl: "",
    })

    // 页面曝光打点
    onetrack("trackPage", {
      uid: Cookies.get("cUserId") || Cookies.get("userId"),
    })

    // 设置截图分享
    // miWear.setOptionButton({
    //   items: [{
    //     type: "share",
    //     shareType: "screenshot"
    //   }]
    // })
  }, [])

  return (
    <div className="page">
      <header className="header"></header>
      <div className="content">小米穿戴活动模板</div>
    </div>
  )
}
