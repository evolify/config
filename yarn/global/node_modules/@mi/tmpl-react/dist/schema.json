{
  "$schema": "http://json-schema.org/schema",
  "id": "MiotSchematicsReactFunction",
  "title": "Miot React Function Options Schema",
  "type": "object",
  "description": "Creates a new generic React Function in the given or default project.",
  "properties": {
    "path": {
      "type": "string",
      "description": "The path of the page.",
      "$default": {
        "$source": "argv",
        "index": 0
      },
      "x-prompt": "What name would you like to use for the page?"
    },
    "cssext": {
      "type": "string",
      "description": "The extension of the style file name.",
      "default": "scss"
    }
  },
  "required": [
    "path"
  ]
}
